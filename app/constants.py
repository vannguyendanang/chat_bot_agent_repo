import os

class Constants:

    CATEGORIES = ["bank_account", "transaction","general_issues", "technical_support"]
    # DB_HOST = os.getenv("DB_HOST", "localhost")        # use host.docker.internal when in Docker
    # DB_USER = os.getenv("DB_USER", "root")
    # DB_PASS = os.getenv("DB_PASS", "Admin25&@")
    # DB_NAME = os.getenv("DB_NAME", "bank_chatbot")
    # DB_PORT = int(os.getenv("DB_PORT", "3306"))